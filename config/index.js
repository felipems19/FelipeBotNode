const storage = require('./storage')
const dialogs = require('./dialogs')
const buttons = require('./buttons')
const steps = require('./steps')
const prompts = require('./prompts')
const { intents, entities } = require('./language')

module.exports = {
  storage,
  dialogs,
  buttons,
  steps,
  prompts,
  intents,
  entities,
}
